#Agl Test Framwork

This repository stores the code of agl-test framework

Refer to the compilation document of AGL community
you can compile "agl test" into the image that you need

##Running

After loading the image onto the board, you can run the test set

###Run all of the test sets:

    agl-test

###Run the test set list of the specified marker

    agl-test -m marker_name

###Run the specified test set

    agl-test test_file_name

##Directory Structure

├── agl-test
├── conftest.py
├── LICENSE
├── plugins
│   ├── agl_test_conf.py
│   ├── agl_test_log.py
│   ├── agl_test_report.py
│   └── agl_test_utils.py
├── pytest.ini
└── README

###agl-test:

The command to start the test

###conftest.py:

Fixture function define

###plugins:

General script file

###pytest.ini:

The file is mainly used for the selection of test sets
